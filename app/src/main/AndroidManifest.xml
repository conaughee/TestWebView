<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="flag.bet.betflag.gecv">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_betflag"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_betflag"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestWebView"
        tools:targetApi="31">
        <activity
            android:name="flag.bet.betflag.gecv.tetris.TetrisActivity"
            android:exported="false"
            android:label="@string/title_activity_tetris"
            android:theme="@style/Theme.TestWebView.NoActionBar" />
        <activity
            android:name="flag.bet.betflag.gecv.MainActivity"
            android:usesCleartextTraffic="true"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestWebView.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>